import React from 'react'

export default function WeatherReducer({state,action}) {
    switch(action.type){
        case 'SET_CITY':
        return (
            <div>WeatherReducer</div>
          )
    }
  
}
