const add = function (x, y) {
    return x + y;
}

//arrow funct
const addd = (x, y) => {
    return x + y;
}
addd(5, 8);

const squre = function (n) {
    return n * n;
}
square(9);

//arow funvt
const sqauree = (n) => {
    return n * n;
}
squaree(9);

//implict function
const sqaureee = (n) => (
    n * n
)
squaree(9);